import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
import {calculateCustomerHealth,buildPilotWorkspace,buildCustomerSuccessWorkspace,validatePilot} from '../src/enterprise-pilot-customer-success.js';
test('health score is calculated from adoption delivery support training and engagement',()=>{const h=calculateCustomerHealth({adoption_score:90,delivery_score:90,support_score:90,training_score:90,engagement_score:90});assert.equal(h.score,90);assert.equal(h.risk,'low')});
test('pilot validation requires dates and owner',()=>{assert.equal(validatePilot({}).ok,false);assert.equal(validatePilot({organization_id:'o',name:'Pilot',start_date:'2026-01-01',end_date:'2026-02-01',owner_id:'u'}).ok,true)});
test('pilot workspace and customer success workspace expose requested modules',()=>{const p=buildPilotWorkspace({pilots:[{status:'running'}],organizations:[{id:'o'}]});assert.equal(p.metrics.active,1);const c=buildCustomerSuccessWorkspace({organizations:[{id:'o',name:'Org'}]});assert.equal(c.organizations.length,1)});
test('routes and frontend pages are wired',()=>{const i=fs.readFileSync(new URL('../src/application.js',import.meta.url),'utf8');assert.match(i,/customer-success\/v2109a\/pilots/);for(const f of ['../../site/app/pilot-management-center.html','../../site/app/customer-success-workspace.html','../../site/admin/founder-customer-success.html','../../site/admin/operations-customer-success.html'])assert.equal(fs.existsSync(new URL(f,import.meta.url)),true)});
