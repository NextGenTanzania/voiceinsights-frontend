(function(){
  const apiBase=()=>window.VI_API_BASE||'https://voiceinsights-api.kitentyatsnp.workers.dev';
  const token=()=>localStorage.getItem('vi_token')||'';
  async function api(path,options={}){const r=await fetch(apiBase()+path,{...options,headers:{'Content-Type':'application/json',...(token()?{Authorization:`Bearer ${token()}`}:{ }),...(options.headers||{})}});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error||`HTTP ${r.status}`);return data;}
  function metric(label,value){return `<div class="iam-metric"><strong>${value}</strong><span>${label}</span></div>`;}
  async function renderIam(){const root=document.getElementById('iam-root');if(!root)return;try{const d=await api('/api/security/v2103a/iam/overview');const m=d.metrics;root.innerHTML=`<section class="iam-hero"><p>VoiceInsights v210.3A</p><h1>Enterprise Identity & Access Center</h1><span>IAM, MFA, SSO, SCIM and scoped API keys.</span></section><div class="iam-grid">${metric('Total users',m.total_users)}${metric('MFA coverage',m.mfa_coverage_pct+'%')}${metric('Without MFA',m.users_without_mfa)}${metric('SSO connections',m.active_sso_connections)}${metric('Active API keys',m.active_api_keys)}</div><div class="iam-panel"><h2>Security controls</h2><div class="iam-actions"><a href="/app/security/mfa.html">Configure MFA</a><a href="/app/security/sso.html">Configure SSO</a><a href="/app/security/scim.html">SCIM Provisioning</a><a href="/app/security/api-keys.html">API Keys</a></div></div><div class="iam-panel"><h2>Role permission model</h2>${d.roles.map(r=>`<details><summary>${r.role} — ${r.permission_count} permissions</summary><p>${r.permissions.join(', ')}</p></details>`).join('')}</div>`;}catch(e){root.innerHTML=`<div class="iam-panel"><h2>Unable to load IAM</h2><p>${e.message}</p></div>`;}}
  window.V2103AIam={api,renderIam};document.addEventListener('DOMContentLoaded',renderIam);
})();
