# API Guide — v200

Public demo v200 endpoint:

`GET /api/public/demo-reports/:id/international-reporting-suite`

Authenticated report v200 endpoint:

`GET /api/reports/:id/international-reporting-suite`
