# Operations

Cloudflare Queues transport AI, transcription, translation, messaging, voice, report generation, exports, notifications and offline synchronization jobs. Built-in adapters call the platform's existing domain services. Failed jobs are classified, retried when transient and recorded in the dead-letter registry when terminal.

Use the operations endpoints and D1 lifecycle tables to inspect queue state, processing latency, failures and replay eligibility.
