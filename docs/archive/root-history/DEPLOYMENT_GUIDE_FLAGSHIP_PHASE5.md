# Deployment — Flagship Report Engine Phase 5

No D1 migration is required. Run backend tests, deploy the Worker, then publish the `site` directory through GitHub/Cloudflare Pages. Purge cache and unregister the old service worker if the previous Report Library remains visible.
