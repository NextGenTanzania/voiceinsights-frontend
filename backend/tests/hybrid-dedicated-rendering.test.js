import test from 'node:test';import assert from 'node:assert/strict';import {flagshipArtifactKey,getPrebuiltFlagshipArtifact,resolveHybridFlagshipArtifact} from '../src/hybrid-dedicated-rendering.js';
test('artifact keys are deterministic and branded',()=>{assert.equal(flagshipArtifactKey('national-human-development','pdf'),'flagship/v1/national-human-development/pdf/voiceinsights-national-human-development.pdf');});
test('prebuilt artifacts are preferred over renderer calls',async()=>{const data=new TextEncoder().encode('PDF');const env={FLAGSHIP_PUBLICATIONS_BUCKET:{get:async key=>({arrayBuffer:async()=>data.buffer,httpMetadata:{contentType:'application/pdf'},customMetadata:{filename:'voiceinsights-test.pdf',checksum:'abc'}})}};const a=await resolveHybridFlagshipArtifact(env,{key:'test',format:'pdf',model:{}});assert.equal(a.source,'governed-artifact-storage');assert.equal(a.filename,'voiceinsights-test.pdf');});
test('missing renderer configuration returns null for controlled legacy fallback',async()=>{assert.equal(await getPrebuiltFlagshipArtifact({},'x','pdf'),null);assert.equal(await resolveHybridFlagshipArtifact({}, {key:'x',format:'pdf',model:{}}),null);});
